# =============================================================================
# requirements.txt — Dependencias completas del proyecto CLV Forecasting
# =============================================================================
# Este archivo incluye TODAS las dependencias del proyecto (dev + prod).
# Para despliegue, usar los archivos específicos:
#   - requirements.api.txt       → imagen Docker del backend FastAPI
#   - requirements.streamlit.txt → imagen Docker del frontend Streamlit
# =============================================================================

# ─── Core: Datos & Computación ───────────────────────────────────────────────
numpy==1.26.4
pandas==2.2.3
scipy==1.17.0
pyarrow==15.0.2              # lectura/escritura de Parquet
python-dateutil==2.9.0.post0
pytz==2025.2
six==1.17.0

# ─── Machine Learning ────────────────────────────────────────────────────────
scikit-learn==1.4.2
joblib==1.4.2
lifetimes==0.11.3            # modelos BG/NBD y Gamma-Gamma para CLV
statsmodels==0.14.4          # regresión y análisis estadístico
patsy==1.0.2                 # fórmulas estadísticas (dep de statsmodels)

# ─── Visualización ───────────────────────────────────────────────────────────
matplotlib==3.9.0
seaborn==0.13.2
plotly==5.22.0

# ─── Backend: FastAPI ────────────────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.0
starlette==0.52.1
pydantic==2.7.1
pydantic-settings==2.3.0
python-dotenv==1.0.1
httpx==0.28.1                # cliente HTTP + requerido por TestClient en tests

# ─── Frontend: Streamlit ─────────────────────────────────────────────────────
streamlit==1.41.1
requests==2.32.3

# ─── Monitoreo: Evidently AI ─────────────────────────────────────────────────
evidently==0.7.20            # detección de data drift (v0.7+ API)

# ─── MLOps: MLflow ───────────────────────────────────────────────────────────
mlflow==3.9.0
dill==0.4.1                  # serialización extendida (dep de mlflow)
cloudpickle==3.1.2

# ─── Testing ─────────────────────────────────────────────────────────────────
pytest==9.0.2
pytest-cov==4.1.0            # cobertura de tests

# ─── Jupyter (desarrollo / notebooks) ────────────────────────────────────────
jupyter_core==5.7.1
ipykernel==6.29.5
ipython==8.26.0

# ─── Utilidades generales ─────────────────────────────────────────────────────
tqdm==4.66.4
PyYAML==6.0.1
click==8.1.7
rich==13.8.1
colorama==0.4.6
certifi==2024.7.4
urllib3==2.2.2
charset-normalizer==3.3.2
idna==3.7
